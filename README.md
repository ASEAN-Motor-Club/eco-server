# Nix Flake for Eco Dedicated Server

## Submitting server config changes
Inside `Configs`, copy the template file to a new file (unless it already exists).
For example, copy `Sleep.eco.template` to `Sleep.eco`.
Then make changes to it, and submit a Pull Request.

